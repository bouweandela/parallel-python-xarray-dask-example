# parallel-python-xarray-dask-example
An example of how to use xarray and dask for parallel computations with Python


## Usage instructions
This repository uses [pixi](pixi.prefix.dev) to manage dependencies.
Run the following command to start the notebook:

```bash
pixi run jupyter lab notebook.ipynb
```
